/**
 * Dashboard — Empty state. No data yet.
 */
export function DashboardPage() {
  return (
    <section className="empty-state">
      <h1>Dashboard</h1>
      <p className="empty-state__message">
        No jobs yet. In the next step, you will load a realistic dataset.
      </p>
    </section>
  );
}
