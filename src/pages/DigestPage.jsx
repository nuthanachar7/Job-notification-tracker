/**
 * Digest — Premium empty state. Future daily summary feature.
 */
export function DigestPage() {
  return (
    <section className="empty-state">
      <h1>Digest</h1>
      <p className="empty-state__message">
        Your daily job summary will arrive here at 9AM. Enable notifications in settings to never miss a match.
      </p>
    </section>
  );
}
